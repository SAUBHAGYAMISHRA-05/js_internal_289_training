// Initialize the application when DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
    // Initialize DOM Handler
    const domHandler = new DOMHandler();
    
    // Add some sample data for testing (optional)
    console.log('Student Registration Form initialized successfully!');
    console.log('Project Requirements:');
    console.log('- HTML Form Structure');
    console.log('- JavaScript Validation');
    console.log('- DOM Manipulation');
    console.log('- Live Output Display');
    
    // Optional: You can add more initialization code here
    // For example, you could pre-fill form for testing:
    // domHandler.elements.name.value = 'Test Student';
    // domHandler.elements.email.value = 'test@example.com';
});